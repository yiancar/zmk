CONFIG_ZMK_TEST_BEHAVIORS=y
CONFIG_ZMK_TEST_STUDIO_RPC_TX=y
# Force the response to wrap and block while the test transport drains three bytes at a time.
CONFIG_ZMK_STUDIO_RPC_TX_BUF_SIZE=8
CONFIG_ZMK_STUDIO_RPC_TX_TIMEOUT_MS=500
